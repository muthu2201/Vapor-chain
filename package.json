{
  "name": "vaporchain",
  "private": true,
  "description": "VaporChain monorepo (proprietary). See LICENSE.",
  "license": "SEE LICENSE IN LICENSE",
  "packageManager": "pnpm@12.6.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r --filter './packages/**' run build && pnpm --filter @vaporchain/web run build",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r --filter './packages/**' run test",
    "test:e2e": "pnpm -r --filter './packages/**' run test:e2e && pnpm --filter @vaporchain/web run test:e2e",
    "gen:abis": "node scripts/gen-abis.mjs"
  }
}
